actions:
  YOUR_ACCOUNT_SLUG/YOUR_PROJECT_SLUG:
    runtime: v2
    sources: tokenPairMonitor
    specs:
      RedStonePriceFeed_WSTETH-ETH:
        description: Check if token pair price for wstETH/ETH is updated at least once every 6 hours.
        function: wstEthEth:monitorWstEthEthFn
        trigger:
          type: periodic
          periodic:
            interval: 1h
        execution_type: parallel
